<table width=100%>
<tr><td>
<h4>${feed.title}</h4>
</td></tr>
% if feed.description:
<tr><td>
  ${feed.description}
</td></tr>
% endif
% if feed.htmlUrl:
<tr><td>
  <a href="${feed.htmlUrl}">${feed.htmlUrl}</a>
</td></tr>
% endif
